#include <stdio.h>
/**
 * main - prints the alphabet
 *
 * Return: return a status code
 */
int main(void)
{

int size;


for (char letter = 'a'; letter <= 'z'; letter++)
putchar(l);
for (char letter = 'A'; letter <= 'Z'; letter++)
putchar(letter);
return (0);
}
