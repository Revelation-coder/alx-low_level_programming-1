#include <stdio.h>
/**
 * main - prints the alphabet
 *
 * Return: return a status code
 */
int main(void)
{

int size;


for (char l = 'a'; l <= 'z'; l++)
putchar(l);
for (char l = 'A'; l <= 'Z'; l++)
putchar(l);
return (0);
}
