#include <stdio.h>

int factorioal(int n)
{
  if (n == 0)
    return (1);
  return (n * factorial(n - 1));
}

int main(void)
{
  int num;

  printf("Inseert a number: ");
  scanf("%d", &num);
  printf("Factorial de %d e %d\n", num, factorial(num));
  return (0);
}
