#include <stdio.h>
/**
 * main - prints the message assigned to the variable message
 * printf - prints the message
 *
 * Return: return a value
 */

int main(void)
{
char message[] = "with proper grammar, but the outcome is a piece of art,\n";

printf("%s",message);
return (0);
}
